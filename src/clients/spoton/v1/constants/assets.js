import { createClientAssets } from '../../../../shared/utils/clientAssets';

export const { clientSlug, version, asset } = createClientAssets('spoton', 'v1');

export const FORM_LOGO = asset('form-logo.svg');
