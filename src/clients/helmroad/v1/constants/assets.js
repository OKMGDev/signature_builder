import { createClientAssets } from '../../../../shared/utils/clientAssets';

export const { clientSlug, version, asset } = createClientAssets('helmroad', 'v1');

export const FORM_LOGO = asset('logo.png');
