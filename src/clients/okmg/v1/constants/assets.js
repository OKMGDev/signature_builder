import { createClientAssets } from '../../../../shared/utils/clientAssets';

export const { clientSlug, version, asset } = createClientAssets('okmg', 'v1');

export const FORM_LOGO = asset('logo-bigger.jpg');
