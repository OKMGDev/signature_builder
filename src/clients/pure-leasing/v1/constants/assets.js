import { createClientAssets } from '../../../../shared/utils/clientAssets';

export const { clientSlug, version, asset } = createClientAssets('pure-leasing', 'v1');

export const FORM_LOGO = asset('pure-logo.png');
